---
description: "[STUB — Phase 1] Ship to Microsoft Store via PWA Builder (PWA → MSIX) or direct MSIX/AppX submission"
---

# /ship-msstore — Ship to Microsoft Store (NOT YET IMPLEMENTED)

This command is **not yet implemented**. It lands in **Phase 1** of the roadmap.

For now:
- Use `/app-gtm-release:ship-advisor` to confirm Microsoft Store fits your distribution mix.
- For PWA → Microsoft Store: PWA Builder (https://www.pwabuilder.com/) generates an MSIX package and walks through Microsoft Partner Center submission.
- For Win32 / native MSIX: see Microsoft Partner Center docs directly.

Roadmap: see README.md → Phases.

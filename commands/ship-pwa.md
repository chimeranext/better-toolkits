---
description: "[STUB — Phase 1] Ship a PWA to multiple app stores via PWA Builder (Microsoft Store, Google Play, App Store) + browser install"
---

# /ship-pwa — Ship a PWA Everywhere (NOT YET IMPLEMENTED)

This command is **not yet implemented**. It lands in **Phase 1** of the roadmap.

For now:
- Use `/app-gtm-release:ship-advisor` to choose the right path for your stack and document target stores.
- For PWA → Microsoft Store: visit https://www.pwabuilder.com/ and run the manual flow.
- For PWA → Google Play / App Store: PWA Builder also packages for both, manually.
- For service worker setup: see the planned skill `pwa-quality` (Phase 1) — until then, use Workbox or `offline-plugin` directly.

Roadmap: see README.md → Phases.

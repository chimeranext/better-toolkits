---
description: "[STUB — Phase 2] Ship a .NET MAUI app to Google Play + App Store + Microsoft Store + Mac App Store"
---

# /ship-maui — Ship a .NET MAUI App (NOT YET IMPLEMENTED)

This command is **not yet implemented**. It lands in **Phase 2** of the roadmap.

For now:
- Use `/app-gtm-release:ship-advisor` to confirm MAUI is your framework and document target stores.
- The Phase 0 `app-gtm-release:store-setup` and `store-listing` skills apply to MAUI today (Play + App Store).
- For Microsoft Store from MAUI: defer to `/ship-msstore` (Phase 1) once available, or use Visual Studio's Store submission flow manually.

Roadmap: see README.md → Phases.

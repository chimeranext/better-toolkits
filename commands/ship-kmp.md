---
description: "[STUB — Phase 2] Ship a Kotlin Multiplatform app to Google Play + App Store"
---

# /ship-kmp — Ship a Kotlin Multiplatform App (NOT YET IMPLEMENTED)

This command is **not yet implemented**. It lands in **Phase 2** of the roadmap.

For now:
- Use `/app-gtm-release:ship-advisor` to confirm KMP is your target framework and document the stores you need.
- The Phase 0 `app-gtm-release:store-setup` and `store-listing` skills are framework-agnostic and apply to KMP today (they cover Play Console + App Store Connect setup).
- For the Android target, the `cicd-setup` and `testing-tracks` skills are reusable. The iOS target requires Xcode-driven steps that will be formalized in `/ship-kmp` itself.

Roadmap: see README.md → Phases.

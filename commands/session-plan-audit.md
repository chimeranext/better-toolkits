---
description: "Auditoría de plan de sesión 1-on-1 contra framework coach/mentor/tutor (Irby 2018)"
argument-hint: "[session-slug]"
---

# /session-plan-audit

Invocar skill `instructional-design-toolkit:session-plan-audit` con `$ARGUMENTS`.

Si `$ARGUMENTS` está vacío: skill lista session plans disponibles.

## Output

`docs/instructional-design/session-plans/{slug}/audits/audit-{YYYY-MM-DD}.md`.

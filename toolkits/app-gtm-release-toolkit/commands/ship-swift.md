---
description: "[STUB — Phase 2.5] Ship a native Swift iOS app to App Store + TestFlight"
---

# /ship-swift — Ship a Swift Native iOS App (NOT YET IMPLEMENTED)

This command is **not yet implemented**. It lands in **Phase 2.5** of the roadmap (depends on Phase 2 iOS generalizations from `/ship-kmp` and `/ship-maui`).

For now:
- Use `/app-gtm-release:ship-advisor` to confirm Swift is your framework.
- The Phase 0 `app-gtm-release:store-setup`, `store-listing`, and `testing-tracks` skills apply to Swift today (App Store Connect + TestFlight).
- For App Store submission via Xcode + Fastlane, defer to existing tooling until `/ship-swift` formalizes the pipeline.

Roadmap: see README.md → Phases.

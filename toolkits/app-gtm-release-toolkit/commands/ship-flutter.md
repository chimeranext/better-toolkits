---
description: "Guided Flutter app launch to Google Play + App Store — full lifecycle from code readiness through production release with validation gates and persistent checkpoints"
argument-hint: "[--what-if | --gate N | --resume]"
---
# /ship-flutter

Read and follow [`references/ship-flutter/protocol.md`](../references/ship-flutter/protocol.md) (protocol SSOT). Behavior is unchanged — only structure moved for multi-harness reuse.

`$ARGUMENTS` are unchanged from the pre-split command.

---
description: "Audit/install/verify OpenCode plugin registration for better-toolkits — one stderr baseline adapter (+ optional npm CLI packages). Does not port Claude hooks.json."
argument-hint: "[audit | install | verify] [--project] [--dry-run] [--also-npm] [--config-dir <path>]"
priority: 75
---

# /opencode-setup: $ARGUMENTS

Read and follow [`references/opencode-setup/protocol.md`](../references/opencode-setup/protocol.md) (protocol SSOT).

`$ARGUMENTS` unchanged from the protocol.

---
description: Append completed work items to today's standup file. Accepts a description, PR number, Linear issue ID, or --reset flag as $ARGUMENTS.
priority: 50
---
# /daily-standup-add-completed

Read and follow [`references/daily-standup-add-completed/protocol.md`](../references/daily-standup-add-completed/protocol.md) (protocol SSOT). Behavior is unchanged — only structure moved for multi-harness reuse.

`$ARGUMENTS` are unchanged from the pre-split command.

---
description: Save full session context to a next-day handoff file so tomorrow's session can pick up exactly where you left off. Accepts a short label for the file as $ARGUMENTS.
priority: 50
---
# /goodnight

Read and follow [`references/goodnight/protocol.md`](../references/goodnight/protocol.md) (protocol SSOT). Behavior is unchanged — only structure moved for multi-harness reuse.

`$ARGUMENTS` are unchanged from the pre-split command.

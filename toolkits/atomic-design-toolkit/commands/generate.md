---
description: "Generate Flutter or Vite component hierarchies for a feature following Atomic Design methodology"
argument-hint: "<feature-name> [:stack]  e.g. rescue-alerts, checkout :vite, profile :flutter"
---
# /generate

Read and follow [`references/generate/protocol.md`](../references/generate/protocol.md) (protocol SSOT). Behavior is unchanged — only structure moved for multi-harness reuse.

`$ARGUMENTS` are unchanged from the pre-split command.

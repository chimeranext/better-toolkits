---
description: "Audit a Flutter monorepo and materialize apps/widgetbook with the atomic taxonomy, web build, and deploy-ready config"
argument-hint: "[ui-package-name]  e.g. keiko_ui, altrupets_ui (optional — auto-detected)"
---
# /widgetbook-setup

Read and follow [`references/widgetbook-setup/protocol.md`](../references/widgetbook-setup/protocol.md) (protocol SSOT). Behavior is unchanged — only structure moved for multi-harness reuse.

`$ARGUMENTS` are unchanged from the pre-split command.

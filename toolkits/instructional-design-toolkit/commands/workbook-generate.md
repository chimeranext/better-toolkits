---
description: Generate one standalone interactive HTML workbook for a whole course from its text classes
argument-hint: "[course-slug] [--scope course|module] [--module module-slug]"
---
# /workbook-generate

Read and follow [`references/workbook-generate/protocol.md`](../references/workbook-generate/protocol.md) (protocol SSOT). Behavior is unchanged — only structure moved for multi-harness reuse.

`$ARGUMENTS` are unchanged from the pre-split command.

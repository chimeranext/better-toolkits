---
description: Bootstrap a Linear workspace with label taxonomy, projects, milestones, and reviewer integrations for a startup or business project. Modes: labels, projects, audit, sync, or clean slate (default). Accepts mode as $ARGUMENTS.
---
# /linear-projects-setup

Read and follow [`references/linear-projects-setup/protocol.md`](../references/linear-projects-setup/protocol.md) (protocol SSOT). Behavior is unchanged — only structure moved for multi-harness reuse.

`$ARGUMENTS` are unchanged from the pre-split command.

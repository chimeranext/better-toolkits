{
  "description": "better-toolkits-bootstrap — stderr preserve ON by default (shared/hooks/stderr). Opt-out via .claude/config/stderr-hooks.json or MNM_DISABLE_STDERR_HOOK=1.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/../hooks/stderr/adapters/claude-pre-bash.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}

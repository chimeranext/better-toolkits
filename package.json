{
  "name": "@lapc506/make-no-mistakes",
  "version": "1.1.0",
  "description": "The disciplined dev lifecycle — implement issues, review PRs, sync releases, test E2E, and manage sessions. OpenCode + Claude Code plugin.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": { "make-no-mistakes": "dist/cli.js" },
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" },
    "./cli": { "import": "./dist/cli.js" }
  },
  "files": ["dist/", "commands/", "agents/", "skills/", "scripts/", "README.md", "LICENSE"],
  "engines": { "node": ">=20.11.0" },
  "publishConfig": { "access": "public" },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean && tsup src/cli.ts --format esm --clean false",
    "test": "echo 'no tests yet' && exit 0",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["opencode", "plugin", "claude-code", "workflow", "linear"],
  "repository": { "type": "git", "url": "git+https://github.com/lapc506/make-no-mistakes-toolkit.git" },
  "author": "Luis Andres Pena Castillo",
  "license": "BSL-1.1",
  "dependencies": {
    "@opencode-ai/plugin": "^0.7.0",
    "jsonc-parser": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
